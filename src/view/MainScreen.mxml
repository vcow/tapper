<?xml version="1.0"?>
<f:StackScreenNavigator xmlns:f="library://ns.feathersui.com/mxml"
                        xmlns:fx="http://ns.adobe.com/mxml/2009" xmlns:screen="view.*"
                        rootScreenID="startScreenItem">

    <f:StackScreenNavigatorItem id="startScreenItem">
        <screen:StartScreen/>
    </f:StackScreenNavigatorItem>

    <f:StackScreenNavigatorItem id="gameScreenItem">
        <screen:GameScreen/>
    </f:StackScreenNavigatorItem>

    <f:StackScreenNavigatorItem id="shopScreenItem">
        <screen:ShopScreen/>
    </f:StackScreenNavigatorItem>
    <fx:Script><![CDATA[
        import feathers.core.PopUpManager;

        import starling.events.Event;

        public function showPopUp(title:String, description:String):void
        {
            var popup:PopUp = new PopUp();
            popup.title = title;
            popup.description = description;
            popup.addEventListener(starling.events.Event.CLOSE, onClosePopUp);
            PopUpManager.addPopUp(popup);
        }

        private static function onClosePopUp(event:starling.events.Event):void
        {
            var popup:PopUp = PopUp(event.target);
            popup.removeEventListener(starling.events.Event.CLOSE, onClosePopUp);
            PopUpManager.removePopUp(popup);
        }
        ]]></fx:Script>
</f:StackScreenNavigator>
