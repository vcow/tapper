<?xml version="1.0"?>
<units>
	<!-- Юнит: id - уникальный идентификатор юнита; index - индекс юнита; name - Название юнита; description - описание юнита;
		price - цена юнита; priceGrowth - прирост цены для каждого следующего приобретаемого юнита, может задаваться в
		процентах; maxCount - максимальное количество юнитов, доступных для покупки (optional); index - индекс в
		иерархии юнитов -->
	<unit id="goy" index="1" name="Гой наёмник" price="50" priceGrowth="10%" maxCount="0">
		<!-- Профит, получаемый от юнита за секунду: value - профит, может быть указан в процентах;
		 	maxCount - максимальное количество срабатываний юнита, после чего он деактивируется (optional) -->
		<pps value="1" maxCount="10"/>
		<!-- Профит, получаемый от юнита за клик: value - профит, может быть указан в процентах;
		 	maxCount - максимальное количество срабатываний юнита, после чего он деактивируется (optional) -->
		<ppc value="1"/>
		<!-- Единовременный получаемый профит: value - количество получаемого профита,
		 	может быть указано в процентах -->
		<p value="100%"/>
		<!-- Действие (должно быть поддержано в коде): iconId - уникальный идентификатор действия;
		 title - название действия (optional); description - описание действия (optional) -->
		<action id="setSkinBronze" title="Set skin bronze" description="???"/>
	</unit>
</units>